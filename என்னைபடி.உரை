# சொற்றொடர்வெளிப்படுத்து.exe -portable
--

If you launch the software from a portable memory 
device, it should launch in portable mode (indicated
in the title bar of the program window).

To enforce portable mode, launch the program with the 
commandline parameter "-portable" (without "").

# In the root directory (Git Bash), enable symlinks for the current repo:

    git config core.symlinks true

# Now run cmd.exe as administrator¹ and cd to the repository root. Make your symlink:

    mkdir .github
    cd .github
    mklink ReadMe.md ..\என்னைபடி.உரை

# You can close cmd.exe now and go back to Git Bash.

    Now commit .github/README.md and push to GitHub. 